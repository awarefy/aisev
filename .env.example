DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=mydb
OPENAI_API_KEY=your_openai_api_key_here
LLM_EVAL_HOST=your-ec2-public-hostname-or-domain

# Docker host configuration (for backend container to access host services)
DOCKER_HOST_NAME=host.docker.internal
DOCKER_HOST_IP=host-gateway
